export async function postErp<T>(resource: string, body: unknown): Promise<T> { const response = await fetch(`/api/v1/${resource}`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error?.message || 'Request failed'); return payload.data as T }
export async function getErp<T>(resource: string, query?: Record<string, string>): Promise<T> { const params = query ? `?${new URLSearchParams(query)}` : ''; const response = await fetch(`/api/v1/${resource}${params}`); const payload = await response.json(); if (!response.ok) throw new Error(payload.error?.message || 'Request failed'); return payload.data as T }
